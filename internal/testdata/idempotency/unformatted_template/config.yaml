# Template intentionally unformatted to test normalization
template: |
  if txn:=newrelic.FromContext({{.Ctx}});txn!=nil{defer txn.StartSegment({{.FuncName|quote}}).End()}
imports:
  - github.com/newrelic/go-agent/v3/newrelic
