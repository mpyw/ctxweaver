template: |
  span, {{.CtxVar}} := tracer.StartSpanFromContext({{.Ctx}}, {{.FuncName | quote}})
  defer span.Finish()
imports:
  - "gopkg.in/DataDog/dd-trace-go.v1/ddtrace/tracer"
packages:
  patterns:
    - ./...
